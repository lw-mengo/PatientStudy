<ui:FluentWindow
  x:Class="PatientStudy.Views.Windows.LoginWindow"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
 xmlns:local="clr-namespace:PatientStudy.Views.Windows"
 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
 Title="Sign in"
 Width="420"
 Height="300"
 mc:Ignorable="d"
 WindowStartupLocation="CenterScreen"
 ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
 ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
  >
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Please sign in" FontSize="18" FontWeight="Bold"/>
            <TextBlock Text="Enter your credentials to continue" Opacity="0.7" Margin="0,6,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1" VerticalAlignment="Top" >
            <TextBlock Text="Username" />
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Text="Password" Margin="0,8,0,0"/>
            <PasswordBox x:Name="PasswordBox" />

            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,6,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right"  Margin="0,12,0,0">
            <ui:Button Content="Cancel" Width="88" Command="{Binding CancelCommand}" />
            <ui:Button Content="Login" Width="88" Command="{Binding LoginCommand}"  />
        </StackPanel>
    </Grid>
</ui:FluentWindow>