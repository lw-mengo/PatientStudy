<ui:FluentWindow
    x:Class="PatientStudy.Views.Windows.LoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="PatientStudy - Sign in"
    Width="460"
    SizeToContent="Height"
    ExtendsContentIntoTitleBar="True"
    WindowBackdropType="Mica"
    WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Title="PatientStudy" Grid.Row="0">
            <ui:TitleBar.Icon>
                <ui:SymbolIcon Symbol="ShieldSettings24" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <Grid Grid.Row="1" Margin="32,10,32,32">
            <StackPanel VerticalAlignment="Center">

                <ui:SymbolIcon Symbol="ContactCard28" FontSize="54" Margin="0,0,0,16"/>
                <TextBlock 
                    Text="欢迎登录" 
                    FontSize="28" 
                    FontWeight="Bold" 
                    HorizontalAlignment="Center" 
                    Margin="0,0,0,8"/>
                <TextBlock 
                    Text="请录入您的凭据以访问系统" 
                    Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" 
                    HorizontalAlignment="Center" 
                    Margin="0,0,0,32"/>

                <ui:Card Padding="24">
                    <StackPanel>
                        <TextBlock Text="用户名" Margin="2,0,0,8" FontWeight="Medium" />
                        <ui:TextBox
                            Icon="{ui:SymbolIcon Symbol=Person24}"
                            PlaceholderText="请输入用户名..."
                            Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                            Margin="0,0,0,20" />

                        <TextBlock Text="密码" Margin="2,0,0,8" FontWeight="Medium" />
                        <ui:PasswordBox 
                            x:Name="PasswordBox"
                            Icon="{ui:SymbolIcon Symbol=LockClosed24}"
                            PasswordChar="●"
                            PlaceholderText="请输入密码..." />

                        <TextBlock 
                            Height="20"
                            Margin="0,8,0,0" 
                            Foreground="IndianRed" 
                            FontSize="12"
                            Text="{Binding ErrorMessage}" />
                    </StackPanel>
                </ui:Card>

                <Grid Margin="0,32,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <ui:Button 
                        Grid.Column="0" 
                        Content="取消" 
                        HorizontalAlignment="Stretch"
                        Margin="0,0,8,0"
                        Padding="0,10"
                        Command="{Binding CancelCommand}" />

                    <ui:Button 
                        Grid.Column="1" 
                        Appearance="Primary" 
                        Content="登录系统" 
                        HorizontalAlignment="Stretch"
                        Margin="8,0,0,0"
                        Padding="0,10"
                        Command="{Binding LoginCommand}" />
                </Grid>
            </StackPanel>
        </Grid>
    </Grid>
</ui:FluentWindow>